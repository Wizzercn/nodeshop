<!DOCTYPE html>
<html style="width:100%;height:100%">
<head>
  <meta charset="utf-8">
  <title></title>
  <link rel="stylesheet" type="text.css" href="/public/xiren/css/css.css">
  <meta content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=0" name="viewport"/>
  <meta content="yes" name="apple-mobile-web-app-capable"/>
  <meta content="black" name="apple-mobile-web-app-status-bar-style"/>
  <meta content="telephone=no" name="format-detection"/>
  <script src="/plugins/jquery-1.11.1.min.js"></script>

</head>
<body
  style="width:100%; height:100%;background:url(/public/xiren/img/bg.jpg) no-repeat center center;background-size:100% 100%; ">
<input type="hidden" id="salesid" value="<%= sales.id %>">
<input type="hidden" id="logid" value="<%= log.id %>">
<input type="hidden" id="wxid" value="<%= sales.wxid %>">
<input type="hidden" id="fromId" value="<%= fromId %>">
<audio src="<%= sales.audiourl||'' %>" id="audio" controls autoplay="autoplay" loop style="display:none;height:0;"></audio>
<div class="land">
			<span class="land_m"><input id="name" type="text" placeholder="输入您的姓名" class="land_text">
			<a id="btn" class="land_sub">去寻找我的新年花语</a>	</span>
</div>
<script type="text/javascript">
  $(document).ready(function () {
    $('audio').get(0).play();
    $("#btn").on("click", function () {
      var name = $("#name").val();
      if (name == "") {
        alert("请填写姓名~~");
      } else {
        $.ajax({
          type: 'POST',
          url: '/public/wx/sales/xiren/join',
          data: {'name': name, "salesid": $("#salesid").val(), "logid": $("#logid").val(), "wxid": $("#wxid").val(), "fromId": $("#fromId").val()},
          success: function (data) {
            if (data.code == 0) {
              window.location.href = '/public/wx/sales/xiren/huayu?salesid=' + $("#salesid").val() + '&logid=' + $("#logid").val() + '&wxid=' + $("#wxid").val();
            } else if (data.code == 2) {
              window.location.href = '/public/wx/sales/xiren/huayu?salesid=' + $("#salesid").val() + '&logid=' + data.msg + '&wxid=' + $("#wxid").val();
            } else{
              alert(data.msg);
            }
          },
          dataType: 'json'
        });
      }
    });
  });
</script>
</body>
</html>
